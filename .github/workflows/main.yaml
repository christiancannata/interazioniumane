on: push
name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
      - name: 🚚 Get latest code
        uses: actions/checkout@v3

      - name: 📂 Sync files
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: access878234082.webspace-data.io
          port: 22
          protocol: ftps
          username: u734623326
          password: ${{ secrets.ftp_password }}
          server-dir: /clickandbuilds/StagingInterazioniUmane/
          dry-run: true
          git-ftp-args: --insecure